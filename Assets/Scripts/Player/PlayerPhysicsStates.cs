using UnityEngine;

public class PlayerPhysicsStates : MonoBehaviour
{

    //public static PlayerPhysicsStates Instance { get; private set; }
    //public bool IsGrounded;

    //private void Awake()
    //{
    //    if (Instance == null || Instance != this)
    //        Destroy(this);
    //    else Instance = this;
    //}
}
